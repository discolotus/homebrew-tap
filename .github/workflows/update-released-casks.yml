name: Update released casks

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    uses: discolotus/release-workflows/.github/workflows/update-homebrew-tap.yml@v1
    with:
      manifest_path: release-sources.json
